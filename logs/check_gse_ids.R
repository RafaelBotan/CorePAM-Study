options(warn=0)
library(arrow)
expr <- read_parquet('Y:/Phd-Genomic-claude/01_Base_Pura_CorePAM/PROCESSED/GSE20685/expression_genelevel_preZ.parquet')
rdy  <- read_parquet('Y:/Phd-Genomic-claude/01_Base_Pura_CorePAM/PROCESSED/GSE20685/analysis_ready.parquet')
s_ids <- setdiff(names(expr), 'gene')
cat('expr sample IDs (first 3):', paste(s_ids[1:3], collapse=', '), '\n')
cat('rdy patient_id (first 3):', paste(rdy$patient_id[1:3], collapse=', '), '\n')
cat('common:', length(intersect(s_ids, rdy$patient_id)), '\n')
cat('expr n samples:', length(s_ids), '\n')
cat('rdy nrow:', nrow(rdy), '\n')
